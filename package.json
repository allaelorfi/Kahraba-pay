{
  "name": "kahraba-pay",
  "private": true,
  "version": "1.0.0",
  "description": "Root scripts to install and run both the backend and frontend of KahrabaPay together",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k -n BACKEND,FRONTEND -c blue,green \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup:db": "npm run db:setup --prefix backend",
    "seed:demo": "node backend/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
